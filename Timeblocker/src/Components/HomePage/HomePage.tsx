import Calendar from "../TimeBlocker/Calendar";
import ToDo from "../TimeBlocker/ToDo";

const HomePage = () => {
    return (
        <main>
            <nav>

            </nav>
            <section>
                <Calendar />
                <ToDo />
            </section>
        </main>
    );
};

export default HomePage;